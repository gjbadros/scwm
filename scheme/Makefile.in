#
SHELL=/bin/sh

@SET_MAKE@

VPATH=@scrdir@
top_srcdir=@top_srcdir@
srcdir=@srcdir@
prefix=@prefix@
exec_prefix=@exec_prefix@
bin_dir=@exec_prefix@/bin
scwm_rc=@scwm_rc@
scwm_name=@scwm_name@
scwm_scheme_dir=@scwm_scheme_mod_dir@/app/scwm

mkinstalldirs=$(SHELL) $(top_srcdir)/mkinstalldirs

INSTALL=@INSTALL@
INSTALL_DATA=@INSTALL_DATA@
RM=@RM@

RC=system${scwm_rc}



all: 

depend:

# XXX - this way of doing it sucks. Fix it later when we figure out Automake.
# Or something.
# This will work with GNU make, but I don't know about other makes --11/10/97 gjb
#INSTALL_SCHEME_FILES = $(wildcard *.scm)
# for i in $(INSTALL_SCHEME_FILES); do \
#    ${INSTALL_DATA} $$i ${scwm_scheme_dir} \
# done
install: 
	$(mkinstalldirs) ${scwm_scheme_dir}
	${INSTALL_DATA} base.scm ${scwm_scheme_dir} 
	${INSTALL_DATA} decor.scm ${scwm_scheme_dir} 
	${INSTALL_DATA} face.scm ${scwm_scheme_dir} 
	${INSTALL_DATA} fvwm-compat.scm ${scwm_scheme_dir} 
	${INSTALL_DATA} optargs.scm ${scwm_scheme_dir}
	${INSTALL_DATA} style-options.scm ${scwm_scheme_dir} 
	${INSTALL_DATA} style.scm ${scwm_scheme_dir} 
	${INSTALL_DATA} wininfo.scm ${scwm_scheme_dir} 
	${INSTALL_DATA} winlist.scm ${scwm_scheme_dir} 
	${INSTALL_DATA} winops.scm ${scwm_scheme_dir} 
	${INSTALL_DATA} std-menus.scm ${scwm_scheme_dir} 
	${INSTALL_DATA} wmconfig-menu.scm ${scwm_scheme_dir} 
	${INSTALL_DATA} image-loaders.scm ${scwm_scheme_dir} 
	${INSTALL_DATA} bincomm.scm ${scwm_scheme_dir} 
	${INSTALL_DATA} fvwm-eval.scm ${scwm_scheme_dir} 
	${INSTALL_DATA} fvwm-module.scm ${scwm_scheme_dir} 

mostlyclean:
	${RM} *~ core *.bak

clean: mostlyclean

distclean: clean





