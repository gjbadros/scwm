(use-modules (app scwm fvwm-module))
(use-modules (app scwm module-types))
(use-modules (app scwm bincomm))
(fvwm2-module-send-window-list (current-output-port))

(define port (current-output-port))
(define body "foo")
(define str "foo")
(define id M_NEW_PAGE)
(def

(define body-length .75)

(uniform-array-write "foo" (current-output-port))
(current-input-port)
(current-error-port)

(array? "foo")
